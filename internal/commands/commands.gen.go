// Code generated by scripts/gen_commands/main.go DO NOT EDIT.
// Source: schema/pr-schema.yaml
//
// This file is regenerated whenever the PR schema changes in CI.
// Do not edit manually — run: go run scripts/gen_commands/main.go schema/pr-schema.yaml internal/commands/commands.gen.go

package commands

import (
	"context"
	"fmt"
	"strconv"

	"github.com/spf13/cobra"

	"github.com/FabianSchurig/bitbucket-cli/internal/client"
	"github.com/FabianSchurig/bitbucket-cli/internal/handlers"
	"github.com/FabianSchurig/bitbucket-cli/internal/output"
)

// NewPRCommand returns the "pr" cobra command with all sub-commands registered.
func NewPRCommand() *cobra.Command {
	prCmd := &cobra.Command{
		Use:   "pr",
		Short: "Manage Bitbucket pull requests",
		Long:  "Commands for listing, creating, reading, and merging Bitbucket pull requests.",
	}

	prCmd.AddCommand(
		newListPullRequestsCmd(),
		newGetPullRequestCmd(),
		newCreatePullRequestCmd(),
		newMergePullRequestCmd(),
	)

	return prCmd
}

// newListPullRequestsCmd returns the "pr list" cobra command.
// operationId: listPullRequests
func newListPullRequestsCmd() *cobra.Command {
	var (
		workspace string
		repoSlug  string
		state     string
		all       bool
	)

	cmd := &cobra.Command{
		Use:   "list",
		Short: "List pull requests",
		Long:  "Returns all pull requests on the specified repository.",
		RunE: func(cmd *cobra.Command, args []string) error {
			if workspace == "" {
				return fmt.Errorf("--workspace is required")
			}
			if repoSlug == "" {
				return fmt.Errorf("--repo is required")
			}

			c, err := client.NewClient()
			if err != nil {
				return err
			}

			return handlers.ListPullRequests(context.Background(), c, handlers.ListPRsInput{
				Workspace: workspace,
				RepoSlug:  repoSlug,
				State:     state,
				All:       all,
			})
		},
	}

	cmd.Flags().StringVar(&workspace, "workspace", "", "Workspace ID or slug (required)")
	cmd.Flags().StringVar(&repoSlug, "repo", "", "Repository slug (required)")
	cmd.Flags().StringVar(&state, "state", "", "Filter by state: OPEN, MERGED, DECLINED, SUPERSEDED")
	cmd.Flags().BoolVar(&all, "all", false, "Traverse all pages (follows 'next' cursor)")

	return cmd
}

// newGetPullRequestCmd returns the "pr get" cobra command.
// operationId: getPullRequest
func newGetPullRequestCmd() *cobra.Command {
	var (
		workspace     string
		repoSlug      string
		pullRequestID string
	)

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Get a pull request",
		Long:  "Returns the specified pull request.",
		RunE: func(cmd *cobra.Command, args []string) error {
			if workspace == "" {
				return fmt.Errorf("--workspace is required")
			}
			if repoSlug == "" {
				return fmt.Errorf("--repo is required")
			}
			if pullRequestID == "" {
				return fmt.Errorf("--id is required")
			}
			id, err := strconv.Atoi(pullRequestID)
			if err != nil {
				return fmt.Errorf("--id must be a positive integer: %w", err)
			}

			c, err := client.NewClient()
			if err != nil {
				return err
			}

			return handlers.GetPullRequest(context.Background(), c, handlers.GetPRInput{
				Workspace:     workspace,
				RepoSlug:      repoSlug,
				PullRequestID: id,
			})
		},
	}

	cmd.Flags().StringVar(&workspace, "workspace", "", "Workspace ID or slug (required)")
	cmd.Flags().StringVar(&repoSlug, "repo", "", "Repository slug (required)")
	cmd.Flags().StringVar(&pullRequestID, "id", "", "Pull request ID (required)")

	return cmd
}

// newCreatePullRequestCmd returns the "pr create" cobra command.
// operationId: createPullRequest
func newCreatePullRequestCmd() *cobra.Command {
	var (
		workspace         string
		repoSlug          string
		title             string
		description       string
		sourceBranch      string
		destinationBranch string
		reviewers         []string
		closeSourceBranch bool
		body              string
	)

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create a pull request",
		Long: `Creates a new pull request.

Flag usage:
  Use --title, --source-branch, --destination-branch, and other flags for simple PRs.
  Use --reviewer (repeatable) with UUID values: --reviewer uuid1 --reviewer uuid2,uuid3
  For full control, pass --body with a raw JSON body (overrides all other flags).`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if workspace == "" {
				return fmt.Errorf("--workspace is required")
			}
			if repoSlug == "" {
				return fmt.Errorf("--repo is required")
			}
			if body == "" && title == "" {
				return fmt.Errorf("--title is required (or use --body for raw JSON)")
			}

			c, err := client.NewClient()
			if err != nil {
				return err
			}

			return handlers.CreatePullRequest(context.Background(), c, handlers.CreatePRInput{
				Workspace:         workspace,
				RepoSlug:          repoSlug,
				Title:             title,
				Description:       description,
				SourceBranch:      sourceBranch,
				DestinationBranch: destinationBranch,
				ReviewerUUIDs:     reviewers,
				CloseSourceBranch: closeSourceBranch,
				Body:              body,
			})
		},
	}

	cmd.Flags().StringVar(&workspace, "workspace", "", "Workspace ID or slug (required)")
	cmd.Flags().StringVar(&repoSlug, "repo", "", "Repository slug (required)")
	cmd.Flags().StringVar(&title, "title", "", "Pull request title")
	cmd.Flags().StringVar(&description, "description", "", "Pull request description")
	cmd.Flags().StringVar(&sourceBranch, "source-branch", "", "Source branch name")
	cmd.Flags().StringVar(&destinationBranch, "destination-branch", "", "Destination branch name")
	cmd.Flags().StringArrayVar(&reviewers, "reviewer", nil, "Reviewer UUID (repeatable, or comma-separated)")
	cmd.Flags().BoolVar(&closeSourceBranch, "close-source-branch", false, "Close source branch after merge")
	cmd.Flags().StringVar(&body, "body", "", "Raw JSON request body (overrides all other flags)")

	return cmd
}

// newMergePullRequestCmd returns the "pr merge" cobra command.
// operationId: mergePullRequest
func newMergePullRequestCmd() *cobra.Command {
	var (
		workspace         string
		repoSlug          string
		pullRequestID     string
		strategy          string
		message           string
		closeSourceBranch bool
	)

	cmd := &cobra.Command{
		Use:   "merge",
		Short: "Merge a pull request",
		Long:  "Merges the specified pull request.",
		RunE: func(cmd *cobra.Command, args []string) error {
			if workspace == "" {
				return fmt.Errorf("--workspace is required")
			}
			if repoSlug == "" {
				return fmt.Errorf("--repo is required")
			}
			if pullRequestID == "" {
				return fmt.Errorf("--id is required")
			}
			id, err := strconv.Atoi(pullRequestID)
			if err != nil {
				return fmt.Errorf("--id must be a positive integer: %w", err)
			}
			if strategy != "" {
				valid := map[string]bool{
					"merge_commit": true,
					"squash":       true,
					"fast_forward": true,
				}
				if !valid[strategy] {
					return fmt.Errorf("--strategy must be one of: merge_commit, squash, fast_forward")
				}
			}

			c, err := client.NewClient()
			if err != nil {
				return err
			}

			return handlers.MergePullRequest(context.Background(), c, handlers.MergePRInput{
				Workspace:         workspace,
				RepoSlug:          repoSlug,
				PullRequestID:     id,
				Strategy:          strategy,
				Message:           message,
				CloseSourceBranch: closeSourceBranch,
			})
		},
	}

	cmd.Flags().StringVar(&workspace, "workspace", "", "Workspace ID or slug (required)")
	cmd.Flags().StringVar(&repoSlug, "repo", "", "Repository slug (required)")
	cmd.Flags().StringVar(&pullRequestID, "id", "", "Pull request ID (required)")
	cmd.Flags().StringVar(&strategy, "strategy", "", "Merge strategy: merge_commit, squash, fast_forward")
	cmd.Flags().StringVar(&message, "message", "", "Commit message for the merge commit")
	cmd.Flags().BoolVar(&closeSourceBranch, "close-source-branch", false, "Delete source branch after merge")

	return cmd
}

// outputFlagName is the shared --output flag used by all commands.
const outputFlagName = "output"

// AddOutputFlag adds the --output flag to a command and wires it to output.Format.
func AddOutputFlag(cmd *cobra.Command) {
	cmd.PersistentFlags().StringVar(&output.Format, outputFlagName, "table",
		"Output format: table, json, id")
}
